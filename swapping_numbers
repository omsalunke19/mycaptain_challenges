#include<stdio.h>
int main()
{
    int a,b,temp;
    printf("Enter Value of a and b: ");
    scanf("%d %d",&a,&b);
    temp=a;
    a=b;
    b=temp;
    printf("After Swapping value of a = %d and b = %d",a,b);
    return 0;
}
